{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "npm run start",
      "type": "chrome",
      "request": "launch",
      "name": "Debug App",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}"
    }
  ]
}